function scrollLeft() {
    document.querySelector(".category-slider").scrollBy({ left: -150, behavior: 'smooth' });
}

function scrollRight() {
    document.querySelector(".category-slider").scrollBy({ left: 150, behavior: 'smooth' });
}
